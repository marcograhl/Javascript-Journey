
(function(){
  
  function select(css){
    return document.querySelector(css);
    };

    const API_URL = 'https://starwars.egghead.training/';
    // console.log("API_URL", API_URL);

    // Promise like a  Pizza Buzzer, you order a pizza and if all goes well u get the pizza when its
    //done

    const responsePromise = fetch(API_URL + "films");// here fetch is a Promise, so we wait till the
    // next step.
    // 3 states a promise can have
    



    

  
}());